# @pingid/vite

Vite tooling

# Install

Currently

```
npm install github:Pingid/vite
```

Add
In Vite, set resolve.conditions: ['ts'] in vite.config.ts.

In tsconfig, set "customConditions": ["ts"].
